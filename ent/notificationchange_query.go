// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"notification-service/ent/notificationchange"
	"notification-service/ent/notificationobject"
	"notification-service/ent/predicate"
	"notification-service/ent/user"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// NotificationChangeQuery is the builder for querying NotificationChange entities.
type NotificationChangeQuery struct {
	config
	ctx                    *QueryContext
	order                  []notificationchange.OrderOption
	inters                 []Interceptor
	predicates             []predicate.NotificationChange
	withActor              *UserQuery
	withNotificationObject *NotificationObjectQuery
	withFKs                bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NotificationChangeQuery builder.
func (ncq *NotificationChangeQuery) Where(ps ...predicate.NotificationChange) *NotificationChangeQuery {
	ncq.predicates = append(ncq.predicates, ps...)
	return ncq
}

// Limit the number of records to be returned by this query.
func (ncq *NotificationChangeQuery) Limit(limit int) *NotificationChangeQuery {
	ncq.ctx.Limit = &limit
	return ncq
}

// Offset to start from.
func (ncq *NotificationChangeQuery) Offset(offset int) *NotificationChangeQuery {
	ncq.ctx.Offset = &offset
	return ncq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ncq *NotificationChangeQuery) Unique(unique bool) *NotificationChangeQuery {
	ncq.ctx.Unique = &unique
	return ncq
}

// Order specifies how the records should be ordered.
func (ncq *NotificationChangeQuery) Order(o ...notificationchange.OrderOption) *NotificationChangeQuery {
	ncq.order = append(ncq.order, o...)
	return ncq
}

// QueryActor chains the current query on the "actor" edge.
func (ncq *NotificationChangeQuery) QueryActor() *UserQuery {
	query := (&UserClient{config: ncq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ncq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ncq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(notificationchange.Table, notificationchange.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, notificationchange.ActorTable, notificationchange.ActorColumn),
		)
		fromU = sqlgraph.SetNeighbors(ncq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNotificationObject chains the current query on the "notificationObject" edge.
func (ncq *NotificationChangeQuery) QueryNotificationObject() *NotificationObjectQuery {
	query := (&NotificationObjectClient{config: ncq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ncq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ncq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(notificationchange.Table, notificationchange.FieldID, selector),
			sqlgraph.To(notificationobject.Table, notificationobject.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, notificationchange.NotificationObjectTable, notificationchange.NotificationObjectColumn),
		)
		fromU = sqlgraph.SetNeighbors(ncq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first NotificationChange entity from the query.
// Returns a *NotFoundError when no NotificationChange was found.
func (ncq *NotificationChangeQuery) First(ctx context.Context) (*NotificationChange, error) {
	nodes, err := ncq.Limit(1).All(setContextOp(ctx, ncq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{notificationchange.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ncq *NotificationChangeQuery) FirstX(ctx context.Context) *NotificationChange {
	node, err := ncq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NotificationChange ID from the query.
// Returns a *NotFoundError when no NotificationChange ID was found.
func (ncq *NotificationChangeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ncq.Limit(1).IDs(setContextOp(ctx, ncq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{notificationchange.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ncq *NotificationChangeQuery) FirstIDX(ctx context.Context) int {
	id, err := ncq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NotificationChange entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NotificationChange entity is found.
// Returns a *NotFoundError when no NotificationChange entities are found.
func (ncq *NotificationChangeQuery) Only(ctx context.Context) (*NotificationChange, error) {
	nodes, err := ncq.Limit(2).All(setContextOp(ctx, ncq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{notificationchange.Label}
	default:
		return nil, &NotSingularError{notificationchange.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ncq *NotificationChangeQuery) OnlyX(ctx context.Context) *NotificationChange {
	node, err := ncq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NotificationChange ID in the query.
// Returns a *NotSingularError when more than one NotificationChange ID is found.
// Returns a *NotFoundError when no entities are found.
func (ncq *NotificationChangeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ncq.Limit(2).IDs(setContextOp(ctx, ncq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{notificationchange.Label}
	default:
		err = &NotSingularError{notificationchange.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ncq *NotificationChangeQuery) OnlyIDX(ctx context.Context) int {
	id, err := ncq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NotificationChanges.
func (ncq *NotificationChangeQuery) All(ctx context.Context) ([]*NotificationChange, error) {
	ctx = setContextOp(ctx, ncq.ctx, "All")
	if err := ncq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NotificationChange, *NotificationChangeQuery]()
	return withInterceptors[[]*NotificationChange](ctx, ncq, qr, ncq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ncq *NotificationChangeQuery) AllX(ctx context.Context) []*NotificationChange {
	nodes, err := ncq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NotificationChange IDs.
func (ncq *NotificationChangeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ncq.ctx.Unique == nil && ncq.path != nil {
		ncq.Unique(true)
	}
	ctx = setContextOp(ctx, ncq.ctx, "IDs")
	if err = ncq.Select(notificationchange.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ncq *NotificationChangeQuery) IDsX(ctx context.Context) []int {
	ids, err := ncq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ncq *NotificationChangeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ncq.ctx, "Count")
	if err := ncq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ncq, querierCount[*NotificationChangeQuery](), ncq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ncq *NotificationChangeQuery) CountX(ctx context.Context) int {
	count, err := ncq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ncq *NotificationChangeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ncq.ctx, "Exist")
	switch _, err := ncq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ncq *NotificationChangeQuery) ExistX(ctx context.Context) bool {
	exist, err := ncq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NotificationChangeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ncq *NotificationChangeQuery) Clone() *NotificationChangeQuery {
	if ncq == nil {
		return nil
	}
	return &NotificationChangeQuery{
		config:                 ncq.config,
		ctx:                    ncq.ctx.Clone(),
		order:                  append([]notificationchange.OrderOption{}, ncq.order...),
		inters:                 append([]Interceptor{}, ncq.inters...),
		predicates:             append([]predicate.NotificationChange{}, ncq.predicates...),
		withActor:              ncq.withActor.Clone(),
		withNotificationObject: ncq.withNotificationObject.Clone(),
		// clone intermediate query.
		sql:  ncq.sql.Clone(),
		path: ncq.path,
	}
}

// WithActor tells the query-builder to eager-load the nodes that are connected to
// the "actor" edge. The optional arguments are used to configure the query builder of the edge.
func (ncq *NotificationChangeQuery) WithActor(opts ...func(*UserQuery)) *NotificationChangeQuery {
	query := (&UserClient{config: ncq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ncq.withActor = query
	return ncq
}

// WithNotificationObject tells the query-builder to eager-load the nodes that are connected to
// the "notificationObject" edge. The optional arguments are used to configure the query builder of the edge.
func (ncq *NotificationChangeQuery) WithNotificationObject(opts ...func(*NotificationObjectQuery)) *NotificationChangeQuery {
	query := (&NotificationObjectClient{config: ncq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ncq.withNotificationObject = query
	return ncq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NotificationChange.Query().
//		GroupBy(notificationchange.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ncq *NotificationChangeQuery) GroupBy(field string, fields ...string) *NotificationChangeGroupBy {
	ncq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NotificationChangeGroupBy{build: ncq}
	grbuild.flds = &ncq.ctx.Fields
	grbuild.label = notificationchange.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.NotificationChange.Query().
//		Select(notificationchange.FieldCreatedAt).
//		Scan(ctx, &v)
func (ncq *NotificationChangeQuery) Select(fields ...string) *NotificationChangeSelect {
	ncq.ctx.Fields = append(ncq.ctx.Fields, fields...)
	sbuild := &NotificationChangeSelect{NotificationChangeQuery: ncq}
	sbuild.label = notificationchange.Label
	sbuild.flds, sbuild.scan = &ncq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NotificationChangeSelect configured with the given aggregations.
func (ncq *NotificationChangeQuery) Aggregate(fns ...AggregateFunc) *NotificationChangeSelect {
	return ncq.Select().Aggregate(fns...)
}

func (ncq *NotificationChangeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ncq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ncq); err != nil {
				return err
			}
		}
	}
	for _, f := range ncq.ctx.Fields {
		if !notificationchange.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ncq.path != nil {
		prev, err := ncq.path(ctx)
		if err != nil {
			return err
		}
		ncq.sql = prev
	}
	return nil
}

func (ncq *NotificationChangeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NotificationChange, error) {
	var (
		nodes       = []*NotificationChange{}
		withFKs     = ncq.withFKs
		_spec       = ncq.querySpec()
		loadedTypes = [2]bool{
			ncq.withActor != nil,
			ncq.withNotificationObject != nil,
		}
	)
	if ncq.withActor != nil || ncq.withNotificationObject != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, notificationchange.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NotificationChange).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NotificationChange{config: ncq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ncq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ncq.withActor; query != nil {
		if err := ncq.loadActor(ctx, query, nodes, nil,
			func(n *NotificationChange, e *User) { n.Edges.Actor = e }); err != nil {
			return nil, err
		}
	}
	if query := ncq.withNotificationObject; query != nil {
		if err := ncq.loadNotificationObject(ctx, query, nodes, nil,
			func(n *NotificationChange, e *NotificationObject) { n.Edges.NotificationObject = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ncq *NotificationChangeQuery) loadActor(ctx context.Context, query *UserQuery, nodes []*NotificationChange, init func(*NotificationChange), assign func(*NotificationChange, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*NotificationChange)
	for i := range nodes {
		if nodes[i].user_notification_changes == nil {
			continue
		}
		fk := *nodes[i].user_notification_changes
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_notification_changes" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ncq *NotificationChangeQuery) loadNotificationObject(ctx context.Context, query *NotificationObjectQuery, nodes []*NotificationChange, init func(*NotificationChange), assign func(*NotificationChange, *NotificationObject)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*NotificationChange)
	for i := range nodes {
		if nodes[i].notification_object_notification_changes == nil {
			continue
		}
		fk := *nodes[i].notification_object_notification_changes
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(notificationobject.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "notification_object_notification_changes" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ncq *NotificationChangeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ncq.querySpec()
	_spec.Node.Columns = ncq.ctx.Fields
	if len(ncq.ctx.Fields) > 0 {
		_spec.Unique = ncq.ctx.Unique != nil && *ncq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ncq.driver, _spec)
}

func (ncq *NotificationChangeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(notificationchange.Table, notificationchange.Columns, sqlgraph.NewFieldSpec(notificationchange.FieldID, field.TypeInt))
	_spec.From = ncq.sql
	if unique := ncq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ncq.path != nil {
		_spec.Unique = true
	}
	if fields := ncq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, notificationchange.FieldID)
		for i := range fields {
			if fields[i] != notificationchange.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ncq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ncq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ncq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ncq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ncq *NotificationChangeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ncq.driver.Dialect())
	t1 := builder.Table(notificationchange.Table)
	columns := ncq.ctx.Fields
	if len(columns) == 0 {
		columns = notificationchange.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ncq.sql != nil {
		selector = ncq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ncq.ctx.Unique != nil && *ncq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ncq.predicates {
		p(selector)
	}
	for _, p := range ncq.order {
		p(selector)
	}
	if offset := ncq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ncq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// NotificationChangeGroupBy is the group-by builder for NotificationChange entities.
type NotificationChangeGroupBy struct {
	selector
	build *NotificationChangeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ncgb *NotificationChangeGroupBy) Aggregate(fns ...AggregateFunc) *NotificationChangeGroupBy {
	ncgb.fns = append(ncgb.fns, fns...)
	return ncgb
}

// Scan applies the selector query and scans the result into the given value.
func (ncgb *NotificationChangeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ncgb.build.ctx, "GroupBy")
	if err := ncgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NotificationChangeQuery, *NotificationChangeGroupBy](ctx, ncgb.build, ncgb, ncgb.build.inters, v)
}

func (ncgb *NotificationChangeGroupBy) sqlScan(ctx context.Context, root *NotificationChangeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ncgb.fns))
	for _, fn := range ncgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ncgb.flds)+len(ncgb.fns))
		for _, f := range *ncgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ncgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ncgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NotificationChangeSelect is the builder for selecting fields of NotificationChange entities.
type NotificationChangeSelect struct {
	*NotificationChangeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ncs *NotificationChangeSelect) Aggregate(fns ...AggregateFunc) *NotificationChangeSelect {
	ncs.fns = append(ncs.fns, fns...)
	return ncs
}

// Scan applies the selector query and scans the result into the given value.
func (ncs *NotificationChangeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ncs.ctx, "Select")
	if err := ncs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NotificationChangeQuery, *NotificationChangeSelect](ctx, ncs.NotificationChangeQuery, ncs, ncs.inters, v)
}

func (ncs *NotificationChangeSelect) sqlScan(ctx context.Context, root *NotificationChangeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ncs.fns))
	for _, fn := range ncs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ncs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ncs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
